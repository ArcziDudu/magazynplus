export interface Product {
  id: number;
  name: string;
  category: string;
  producer: string;
  price: number;
  quantity: number;
  description: string;
  availability: boolean;
  productNumber: string;
  imageLink: string;
}
